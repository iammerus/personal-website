---
import mupsLogo from '../assets/img/mups-logo-adaptive.svg'

/**
 * Logo Component
 *
 * @description MUPS Logo - Modern, clean logo with adaptive dark mode support
 */
interface Props {
  /**
   * Whether to load the image eagerly or lazily
   * @default 'eager'
   */
  loading?: 'eager' | 'lazy'
  /**
   * Custom class for styling
   */
  class?: string
}

const { loading = 'eager', class: className = '' }: Props = Astro.props
---

<a href="/" class={`flex items-center !no-underline hover:opacity-80 transition-opacity duration-200 ${className}`}>
  <img 
    src={mupsLogo.src} 
    alt="MUPS - Melvin Mupondori" 
    class="h-18 w-auto text-gray-900 dark:text-white"
    loading={loading}
  />
</a>

<style lang="scss">
  a {
    color: var(--foreground-color);
  }
</style>
